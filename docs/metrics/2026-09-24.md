# Metrics snapshot — 2026-09-24

**What this is:** the numbers [`docs/metrics.md`](../metrics.md) describes,
read once on 2026-09-24 (UTC) with its own commands, at `main` = `a4f25b4`.
This is a record of one day, not a live figure. Rerun the commands in
`docs/metrics.md` for the current values; do not edit this file to update it.

`docs/metrics.md` scopes its tables to PRs #31–#40 so that they stay
reproducible. This snapshot drops that range and reads the whole history.

## PR acceptance, all time

| metric | value |
|---|---|
| PRs merged | 65 |
| PRs closed without merging | 0 |
| PRs open | 0 |
| median time open, merged PRs | 47 min |
| opened by Danathar | 48 |
| opened by `app/danathar-atomic-hive` (Hive) | 17 |

The same caveat as `docs/metrics.md` applies, more strongly: nothing has ever
been declined, so a 65/65 acceptance rate says who opens pull requests here,
not how good they are. The 17 Hive pull requests are the closest thing to an
independent author, and they were merged by the same maintainer.

## Review findings, last 20 merged PRs (#101–#132)

| | |
|---|---|
| PRs with at least one Codex finding | 3 (#125, #127, #128) |
| inline findings | 5 |
| findings embedded in a review body | 0 |

`docs/metrics.md` warns that findings can arrive in a review body as well as
inline. In this window every Codex review body was the standard wrapper text
with no findings in it, so the inline count is the total.

## CI

| metric | value |
|---|---|
| last 30 `ci.yml` runs | 29 success, 1 failure |
| coverage gate | 55% (`--cov-fail-under`) |
| coverage actual | 100% at `a4f25b4` |
